version: '3.7'
services:
  POSTGRES:
    image: postgres:12.1
    ports:
    - '5432:5432'
    volumes:
    - ./data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=app
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=9mREsvXD